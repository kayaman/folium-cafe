{
  "name": "folium-cafe-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Folium Café Lambda backend (Function URL)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "zip": "rm -f function.zip && zip -rq function.zip src node_modules package.json"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.1068.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-presigned-post": "^3.1068.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "@aws-sdk/client-ssm": "^3.600.0",
    "aws-jwt-verify": "^5.2.1"
  }
}
